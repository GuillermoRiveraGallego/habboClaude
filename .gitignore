# config.js contiene la Publishable Key de Supabase, que es PÚBLICA
# por diseño (la protege RLS) y no es un secreto. Por eso SÍ se
# versiona: así Supabase funciona también en el despliegue (GitHub
# Pages, etc.). Lo que NUNCA debe subirse es la service_role key
# (que este proyecto no usa).
